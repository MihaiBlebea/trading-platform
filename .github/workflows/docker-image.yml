name: Docker Image CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - name: Set outputs
      id: vars
      run: echo ${GITHUB_SHA::8}
        
    - uses: actions/checkout@v2
    - name: Build the Docker image
      run: docker build . --file Dockerfile --tag serbanblebea:${GITHUB_SHA::8}
      
    - name: Docker Push
      uses: opspresso/action-docker@v0.4.0
      env:
          USERNAME: ${{ secrets.DOCKER_USERNAME }}
          PASSWORD: ${{ secrets.DOCKER_PASSWORD }}
          IMAGE_NAME: serbanblebea
          TAG_NAME: ${GITHUB_SHA::8}
